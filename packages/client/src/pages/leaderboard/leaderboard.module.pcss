.leaderboard {
  padding: 80px 85px 50px;
}

.topLeaders {
  display: flex;
  justify-content: center;
  align-items: flex-end;
  gap: 45px;
  margin-bottom: 32px;
}

.leadersList {
  & + & {
    margin-top: 45px;
  }
}
