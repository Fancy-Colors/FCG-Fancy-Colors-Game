.wrapper {
  --wrapper-offset: 24px;

  padding: var(--wrapper-offset);
}

.inner {
  display: flex;
  align-items: center;
  justify-content: center;
  height: calc(100vh - var(--wrapper-offset) * 2);
  padding-bottom: 10vh;
}
