.score {
  white-space: nowrap;
  display: flex;
  align-items: center;
  color: var(--color-accent-primary);

  svg {
    margin-right: 4px;
  }
}

@media (max-width: 768px) {
  .score svg {
    width: 16px;
    height: 16px;
  }
}
