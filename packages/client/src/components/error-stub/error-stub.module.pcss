.stubContainer {
  text-align: center;
  margin: 0 auto;
}

.title {
  color: var(--color-accent-primary, #6644ecff);
  font-weight: 600;
  font-size: 195px;
}

.message {
  color: var(--color-dark-primary, #0f101bff);
  margin-bottom: 45px;
  font-weight: 400;
}
