.stubContainer {
  text-align: center;
  margin: 0 auto;
}

.title {
  color: var(--color-accent-primary);
  font-weight: 600;
  font-size: 195px;
}

.subTitle {
  color: var(--color-dark-primary);
  margin-bottom: 45px;
  font-weight: 400;
}
