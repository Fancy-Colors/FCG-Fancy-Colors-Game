.uploadField {
  position: relative;
}

.button {
  margin-bottom: 8px;
}

.hint {
  font-size: 15px;
  margin-bottom: 0;
  margin-top: 0;
  color: var(--text-color-subtle);
}

@media (max-width: 768px) {
  .uploadField {
    text-align: center;
  }

  .hint {
    font-size: 13px;
  }
}
